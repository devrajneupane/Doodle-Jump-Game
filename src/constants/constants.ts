export const DIMENSIONS = {
  CANVAS_WIDTH: 640,
  CANVAS_HEIGHT: 1024,
};

export const STATES = ["start", "play", "end"];

export const PLAYER_WIDTH = 120;
export const PLAYER_HEIGHT = 120;

export const PLATFORM_WIDTH = 120;
export const PLATFORM_HEIGHT = 34;
export const PLATFORM_GAP = 100;

export const SPEED = 5;
export const GRAVITY = 5;

export const MAX_JUMP_HEIGHT = 150;
export const START_JUMP_HEIGHT = 140;

export const FPS = 60;
